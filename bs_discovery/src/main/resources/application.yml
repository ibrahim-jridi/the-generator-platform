
spring:
  application:
    name: discovery
  sleuth:
    enabled: false

management:
  tracing:
    sampling:
      probability: 1.0
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
